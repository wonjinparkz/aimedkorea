<?xml version="1.0" encoding="UTF-8"?>
<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="100" fill="#1e40af"/>
  
  <!-- Medical Cross -->
  <g transform="translate(256, 256)">
    <!-- Vertical bar -->
    <rect x="-50" y="-140" width="100" height="280" fill="white" rx="10"/>
    <!-- Horizontal bar -->
    <rect x="-140" y="-50" width="280" height="100" fill="white" rx="10"/>
  </g>
  
  <!-- AK Text -->
  <text x="256" y="420" font-family="Arial, sans-serif" font-size="48" font-weight="bold" text-anchor="middle" fill="white">AK</text>
</svg>